services:
  mongo:
    image: mongo:7
    container_name: guardianswarm-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - guardianswarm-data:/data/db

volumes:
  guardianswarm-data:
