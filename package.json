{
  "name": "guardianswarm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "frontend": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "backend": "cd backend && .venv/bin/uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "app": "npm --prefix frontend run build && cd backend && .venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000",
    "backend:install": "cd backend && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "install:frontend": "npm --prefix frontend install"
  }
}
